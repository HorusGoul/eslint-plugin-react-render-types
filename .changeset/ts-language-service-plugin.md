---
"eslint-plugin-react-render-types": minor
---

Add TypeScript Language Service Plugin that suppresses unused import diagnostics for components referenced in `@renders` annotations. Enable in `tsconfig.json` with `{ "plugins": [{ "name": "eslint-plugin-react-render-types/language-service-plugin" }] }`.
