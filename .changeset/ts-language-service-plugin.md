---
"eslint-plugin-react-render-types": minor
---

Add TypeScript Language Service Plugin that prevents IDEs from greying out or auto-removing imports referenced in `@renders` annotations. Enable in `tsconfig.json` with `{ "plugins": [{ "name": "eslint-plugin-react-render-types/language-service-plugin" }] }`.
